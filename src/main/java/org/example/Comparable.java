package org.example;
/**
 * Interfaz que Compara dos objetos
 */
public interface Comparable<T> {
    /**
     * Se declara método que se usa para
     * comparar dos objetos y decidir si uno
     * es mayor a otro
     */
    public int compareTo();
}